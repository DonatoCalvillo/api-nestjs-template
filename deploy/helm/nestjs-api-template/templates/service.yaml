apiVersion: v1
kind: Service
metadata:
  name: {{ include "nestjs-api-template.fullname" . }}
  labels:
    {{- include "nestjs-api-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nestjs-api-template.selectorLabels" . | nindent 4 }}
