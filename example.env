# Environment
NODE_ENV=development|production|test
PORT=3000

# API keys (service-to-service)
API_KEYS_ENABLED=true

# Auth extended (email verification, MFA, encryption)
APP_ENCRYPTION_KEY=dev-encryption-key-change-in-production-32chars
# APP display name (Swagger title, etc.)
APP_NAME="NestJS API Template"

# Authentication (JWT + password hashing)
# Production: JWT_*_SECRET required, minimum 32 characters
BCRYPT_ROUNDS=12

# Redis cache (requires REDIS_URL)
CACHE_ENABLED=false
CACHE_TTL_SECONDS=300

# Security - CORS
# Production: CORS_ORIGINS cannot be * — set explicit origins (e.g. https://app.example.com)
CORS_CREDENTIALS=false
CORS_ENABLED=true
CORS_ORIGINS=*

# Database (use 5433 if you have local Postgres on 5432, e.g. Homebrew)
DB_DATABASE=postgres
DB_HOST=localhost
DB_PASSWORD=postgres
DB_PORT=5433
DB_USERNAME=postgres
# DB_SSL=true
# DB_POOL_MAX=20
# DB_POOL_IDLE_TIMEOUT_MS=30000
# DB_MIGRATIONS_RUN=false  # opt-in true in containers; alternative: pnpm migration:run

# Auth extended (email verification)
EMAIL_VERIFICATION_TTL_HOURS=24

# Health checks (Terminus)
HEALTH_DISK_PATH=/
HEALTH_DISK_THRESHOLD_PERCENT=0.9

# Security - HTTP headers (Helmet)
HELMET_ENABLED=true

# HTTP Resilience - outbound API calls
HTTP_CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
HTTP_CIRCUIT_BREAKER_RESET_TIMEOUT_MS=30000
HTTP_RESILIENCE_ENABLED=true
HTTP_RETRY_BACKOFF_MULTIPLIER=2
HTTP_RETRY_DELAY_MS=500
HTTP_RETRY_MAX_ATTEMPTS=3
HTTP_TIMEOUT_MS=5000
# HTTP - inbound limits
# HTTP_BODY_LIMIT=1mb
# HTTP_REQUEST_TIMEOUT_MS=30000

# Idempotency (safe retries via Idempotency-Key header on mutating requests)
# See docs/features/reliability/idempotency.md
IDEMPOTENCY_CLEANUP_CRON="0 */6 * * *"
IDEMPOTENCY_ENABLED=true
IDEMPOTENCY_TTL_HOURS=24

# Security - IP filtering (allowlist)
IP_ALLOWLIST=127.0.0.1,::1
IP_FILTER_ENABLED=false

# Authentication (JWT)
JWT_ACCESS_EXPIRES_IN=15m
JWT_ACCESS_SECRET=dev-access-secret-change-in-production-32chars
JWT_REFRESH_EXPIRES_IN=7d
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production-32chars

# Logging (LOG_LEVEL applies in production JSON mode; dev uses pino-pretty)
# Valid values: fatal, error, warn, info, debug, trace, silent
LOG_LEVEL=info

# Outbox relay (publish domain events to external broker)
# See docs/features/reliability/domain-events-and-outbox.md
MESSAGE_BROKER_ADAPTER=noop

# Prometheus - metrics
METRICS_ENABLED=true
# METRICS_IP_FILTER_ENABLED=true  # defaults to true in production
METRICS_IP_ALLOWLIST=127.0.0.1,::1
# Add Prometheus/Grafana scraper IPs or pod CIDRs in production/K8s

# Auth extended (MFA)
MFA_TOKEN_EXPIRES_IN=5m

# OIDC client (social / enterprise login)
OIDC_ENABLED=false
OIDC_GOOGLE_CLIENT_ID=
OIDC_GOOGLE_CLIENT_SECRET=
OIDC_GOOGLE_ISSUER=https://accounts.google.com
OIDC_GOOGLE_REDIRECT_URI=http://localhost:3000/api/v1/auth/oidc/google/callback

# OpenTelemetry - distributed tracing
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces
OTEL_SERVICE_NAME=nestjs-api-template
OTEL_TRACES_ENABLED=true

# Outbox relay (publish domain events to external broker)
OUTBOX_RECLAIM_ENABLED=true
OUTBOX_RELAY_BATCH_SIZE=50
OUTBOX_RELAY_CRON="*/5 * * * * *"
OUTBOX_RELAY_ENABLED=false
# Multi-instance coordination (Redis)
# Local host: redis://localhost:6379
# Docker Compose: redis://redis:6379 (start with: docker compose up -d db redis)
# REDIS_URL=redis://redis:6379
# THROTTLE_STORAGE=redis
OUTBOX_RELAY_LOCK=memory
OUTBOX_RELAY_LOCK_TTL_SECONDS=120
OUTBOX_RELAY_MAX_ATTEMPTS=5
OUTBOX_STALE_PROCESSING_SECONDS=300

# Auth extended (password reset)
PASSWORD_RESET_TTL_HOURS=1

# RFC 7807 Problem Details
PROBLEM_TYPE_BASE_URL=https://api.example.com/problems/

# Auth extended (email verification)
REQUIRE_EMAIL_VERIFICATION=false

# Graceful shutdown (max wait for in-flight requests before force exit)
SHUTDOWN_DRAIN_TIMEOUT_MS=30000

# File storage (local or S3/MinIO)
STORAGE_DRIVER=local
STORAGE_LOCAL_PATH=./uploads
# STORAGE_DRIVER=s3
# S3_BUCKET=uploads
# S3_REGION=us-east-1
# S3_ENDPOINT=http://localhost:9000

# API documentation (Swagger at /api/docs)
# Defaults to true in development, false in production
SWAGGER_ENABLED=true

# Security - Rate limiting
THROTTLE_ENABLED=true
THROTTLE_LIMIT=100
THROTTLE_STORAGE=memory
THROTTLE_TTL=60

TRUST_PROXY=false
